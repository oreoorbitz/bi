id = "bi#117"
title = "E2E coverage: picker, text modal, cancel paths"
status = "Done"
kind = "Feat"
area = "tui"
body = """
Extend the pty e2e harness past the editor submit: SelectList pick by arrows+Enter, askText modal, Esc/Ctrl-C cancel resolving null-or-cancel contract, Ctrl-D on empty. Accept: (1) each path asserted byte-exact under kitty press+release events, (2) cancel paths leave stdin in a state the next prompt proves clean.

Evidence: drill(e2e-pty)

Verdict 2026-09-05: picker-select (/model Down+Enter → `backend now p/m`), picker-cancel (/resume over 2 seeded sessions, Esc keeps list, re-prompt bursts=3), prompt-cancel (Esc → hint + re-prompt bursts=2), ctrld-eof (EOF kept msg, exit 0) — all green 3x consecutive inside the 35-ok suite. Sends are burst-gated ([gate, offsetMs]) after absolute send times flaked under load (prompt-cancel bursts=1)."""
