id = "bi#92"
title = "Keybinding hints chrome (startup header + dialog footers)"
status = "Open"
kind = "Feat"
area = "tui"
body = """
Pi paints a built-in header (logo + keybinding hints + changelog,
interactive-mode.ts) and per-dialog key footers via
keybinding-hints.ts (formatKeyText/keyHint, darwin option-label).
Bi's REPL opens with a bare session line — new users get no
affordances. Port the hint formatters into BAML (platform-aware
labels, baml-testable) and paint a startup header + dialog footers
on the host. Acceptance: first paint shows hints + latest changelog
line; hints track remapped bindings (bi#91); pipes stay clean.
"""

[[edge]]
from = "bi#92"
to = "bi#26"
kind = "DependsOn"

[[edge]]
from = "bi#92"
to = "bi#32"
kind = "DependsOn"
