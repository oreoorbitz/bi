id = "bi#85"
title = "Fuzzy search in selectors (model + session lists)"
status = "Open"
kind = "Feat"
area = "tui"
body = """
Pi ranks selector rows by fuzzy text (model-search.ts
getModelSearchText/getModelSelectorSearchText, provider-prefixed
ranking; session-selector-search.ts). Bi has prefix-only Tab
completion and unfiltered numbered lists — unusable at 50+ models.
Port the search-text builders + ranking into BAML (testable match),
host owns the filter row. Acceptance: typing in /model and /resume
narrows the list; `xai/grok` ranks the provider-prefixed row first;
empty query lists all; pipes keep today's full list.
"""

[[edge]]
from = "bi#85"
to = "bi#26"
kind = "DependsOn"

[[edge]]
from = "bi#85"
to = "bi#69"
kind = "DependsOn"
