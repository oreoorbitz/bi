id = "bi#97"
title = "Compaction-summary message rendering"
status = "Open"
kind = "Feat"
area = "tui"
body = """
Pi renders a compaction summary block
(compaction-summary-message.ts): what folded, tokens reclaimed,
where to resume. Bi compacts (bi#11) by splicing the summary into
context silently — the transcript never shows that a compaction
happened or what it kept. Port the summary block next to the bi#71
diff work. Acceptance: every auto- or manual compaction emits a
transcript block with folded-turn count and reclaimed tokens; the
block replays from the session file on /resume.
"""

[[edge]]
from = "bi#97"
to = "bi#26"
kind = "DependsOn"

[[edge]]
from = "bi#97"
to = "bi#27"
kind = "DependsOn"
