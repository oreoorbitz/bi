id = "bi#105"
title = "Theme parity: custom theme files + interactive selector with live preview"
status = "Open"
kind = "Feat"
area = "tui"
body = """
Pi themes are files (theme/dark.json, light.json, theme-schema.json,
theme-controller.ts) picked through theme-selector.ts with live
preview. Bi has three hardcoded themes (default/light/none) and
verb-only /theme (list/preview/set) — no custom themes, no
interactive pick. Port the schema + file loading (BAML validates,
host owns FS) and the selector with preview behind bi#69.
Acceptance: a theme JSON validates against the schema; /theme
previews live before commit; malformed files refuse with row-level
reasons; pipes keep verb behavior.
"""

[[edge]]
from = "bi#105"
to = "bi#26"
kind = "DependsOn"

[[edge]]
from = "bi#105"
to = "bi#33"
kind = "DependsOn"

[[edge]]
from = "bi#105"
to = "bi#69"
kind = "DependsOn"
