id = "bi#118"
title = "E2E gates: npm wiring, docs, optional live-LLM smoke"
status = "Done"
kind = "Feat"
area = "tui"
body = """
Wire the pty e2e harness into development gates: npm script, short docs (how to run, how to add a scenario), plus an env-key-gated live-LLM smoke variant for the full run loop. Accept: (1) one command runs the mock-backend suite green with no key, (2) live variant skips cleanly without a key, (3) docs state what symptom coverage each scenario gives.

Evidence: drill(e2e-pty)

Verdict 2026-09-05: `npm run test:e2e` green with no key (live-smoke prints SKIP without BI_E2E_LIVE_KEY, exit 0); docs at bi/scripts/E2E.md (run/add-scenario/scenario→symptom table/red-check record); per-scenario argv (`node scripts/e2e-pty.mjs picker-select`). `npm test` untouched (133 oks) — e2e stays a separate gate."""
